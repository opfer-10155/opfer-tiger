(library
  (name syntax)
  (libraries core semantic)
  (preprocess
    (staged_pps ppx_deriving.show ppx_deriving.eq ppx_import))
  (flags (:standard -warn-error -a)))

(ocamllex lexer)

(menhir
  (modules parser)
  (flags --explain))
